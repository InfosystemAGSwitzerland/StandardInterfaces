400:
  #DEV: Generische Fehler mit 400 code und Error-Return zurückgeben. (Alle nicht mit einem anderen return-code definierten Fälle)
  description: Invalid input, object invalid
  content:
    application/json:
      schema:
        $ref: 'https://github.com/InfosystemAGSwitzerland/performisInterfaces/blob/master/Common/CommonComponents-3.0.0.yam#/Error'
401:
  description: Basic Authentication failed
403:
  description: Requestor has insufficient rights
404:
  description: No Record found
  #DEV: wird auch im Fall eines PUT zurückgegeben, wenn der Record zur internenID des Aufrufs nicht existiert.
Default:
  description: Generic Error
  content:
    application/json:
      schema:
        $ref: 'https://github.com/InfosystemAGSwitzerland/performisInterfaces/blob/master/Common/CommonComponents-3.0.0.yaml#/Error'